let gameName = "spiderman"

gameName = "GTA5"

console.log(gameName);

const userName = "matrix"

// userName = "Maitrik"

// At first most of the programmers declare all the variables using const and then if going froward they think that the value in that variable is needed to be changed then they change that const into let. 